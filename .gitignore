# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/
next-env.d.ts

# Production
dist/

# Env — never commit real credentials. Keep only the template.
.env
.env.local
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Local embedding model cache (BGE/MiniLM via @xenova/transformers)
.cache/
.models/
.xenova/
# Generated by scripts/download-bge-model.mjs during the production build.
models/

# Datasets — dev/validation only, never committed (≈746MB raw resumes/postings/O*NET)
Datasets/

# Validation harness intermediates (derived from gitignored Datasets) + generated figures.
# field_profiles.json + fixtures/ ARE committed; resumes/results/metrics/PNGs are not.
scripts/validation/.artifacts/

# Report figures regenerated by reports/build_reports.py (the PDFs themselves ARE committed).
reports/figs/

# Testing
coverage/
.vitest/

# Real-mode verification artifacts (local evidence only — never commit)
server.log
live-verification-output.txt
live-usage-lines.txt

# Python (offline plane: scripts/ingestion, scripts/validation)
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# Misc
.DS_Store
*.tsbuildinfo
*.log
.vercel
.idea/
.vscode/
.claude/
.claude/
